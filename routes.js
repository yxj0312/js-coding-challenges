import VideoPlayer from './components/VideoPlayer'

export default {
    mode: 'history',

    routes: [
        {
            path: '/challenge-5',
            component: VideoPlayer
        },
    ]
}